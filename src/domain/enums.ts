export enum TaskType {
  Bug = "Bug",
  Feature = "Feature",
  Research = "Research",
  Chore = "Chore",
  Epic = "Epic",
  Story = "Story",
}

export enum TaskStatus {
  New = "New",
  Ready = "Ready",
  InProgress = "InProgress",
  Done = "Done",
  Blocked = "Blocked",
}

export enum Priority {
  Low = 1,
  Medium = 2,
  High = 3,
  Critical = 4,
}
